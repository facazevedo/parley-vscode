{
  "name": "parley-vscode",
  "displayName": "Parley for VS Code",
  "description": "Interact with MIT Parley agents from Visual Studio Code with safe context sharing and diff review.",
  "version": "0.33.0",
  "publisher": "mit-parley-community",
  "license": "MIT",
  "icon": "resources/icon.png",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:parley.chatView"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "parley",
          "title": "Parley",
          "icon": "resources/parley.svg"
        }
      ]
    },
    "views": {
      "parley": [
        {
          "type": "webview",
          "id": "parley.chatView",
          "name": "Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "parley.setApiKey",
        "title": "Parley: Set API Key",
        "category": "Parley"
      },
      {
        "command": "parley.askSelection",
        "title": "Parley: Ask About Selection",
        "category": "Parley"
      },
      {
        "command": "parley.explainFile",
        "title": "Parley: Explain Current File",
        "category": "Parley"
      },
      {
        "command": "parley.refactorSelection",
        "title": "Parley: Refactor Selection",
        "category": "Parley"
      },
      {
        "command": "parley.generateTests",
        "title": "Parley: Generate Tests",
        "category": "Parley"
      },
      {
        "command": "parley.fixDiagnostics",
        "title": "Parley: Fix Diagnostics",
        "category": "Parley"
      },
      {
        "command": "parley.suggestTerminalCommand",
        "title": "Parley: Suggest Terminal Command",
        "category": "Parley"
      },
      {
        "command": "parley.generateImage",
        "title": "Parley: Generate Image",
        "category": "Parley"
      },
      {
        "command": "parley.generateCommitMessage",
        "title": "Parley: Generate Commit Message",
        "category": "Parley"
      },
      {
        "command": "parley.toggleInlineCompletion",
        "title": "Parley: Toggle Inline Completion",
        "category": "Parley"
      },
      {
        "command": "parley.setTokenLimit",
        "title": "Parley: Set Token Limit",
        "category": "Parley"
      },
      {
        "command": "parley.showUsage",
        "title": "Parley: Show Usage",
        "category": "Parley"
      },
      {
        "command": "parley.runDiagnostics",
        "title": "Parley: Run Diagnostics",
        "category": "Parley"
      },
      {
        "command": "parley.initProjectRules",
        "title": "Parley: Init Project Rules",
        "category": "Parley"
      },
      {
        "command": "parley.reconnectMcp",
        "title": "Parley: Reconnect MCP Servers",
        "category": "Parley"
      },
      {
        "command": "parley.newConversation",
        "title": "Parley: New Conversation",
        "category": "Parley"
      },
      {
        "command": "parley.openConversationsFolder",
        "title": "Parley: Open Conversations Folder",
        "category": "Parley"
      },
      {
        "command": "parley.openDebugLog",
        "title": "Parley: Open Debug Log",
        "category": "Parley"
      },
      {
        "command": "parley.exportConversation",
        "title": "Parley: Export Conversation",
        "category": "Parley"
      },
      {
        "command": "parley.compactConversation",
        "title": "Parley: Compact Conversation",
        "category": "Parley"
      },
      {
        "command": "parley.regenerate",
        "title": "Parley: Regenerate Last Response",
        "category": "Parley"
      },
      {
        "command": "parley.openPastConversation",
        "title": "Parley: Open Past Conversation",
        "category": "Parley"
      },
      {
        "command": "parley.inlineEdit",
        "title": "Parley: Edit Selection (Inline)",
        "category": "Parley"
      },
      {
        "command": "parley.revertLastEdit",
        "title": "Parley: Revert Last Edit",
        "category": "Parley"
      },
      {
        "command": "parley.revertAll",
        "title": "Parley: Revert All Edits",
        "category": "Parley"
      },
      {
        "command": "parley.openChatWindow",
        "title": "Parley: Open Chat Window",
        "category": "Parley"
      },
      {
        "command": "parley.signOut",
        "title": "Parley: Sign Out",
        "category": "Parley"
      }
    ],
    "keybindings": [
      {
        "command": "parley.inlineEdit",
        "key": "ctrl+alt+k",
        "mac": "cmd+alt+k",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Parley",
      "properties": {
        "parley.endpoint": {
          "type": "string",
          "default": "https://parley.api.mit.edu/v1",
          "markdownDescription": "Base URL of the OpenAI-compatible MIT Parley API."
        },
        "parley.accountId": {
          "type": "string",
          "default": "",
          "markdownDescription": "Your Parley account id (e.g. `acc_…`), used by **`Parley: Show Usage`** to fetch real billed spend. Find it in the Parley Admin Portal under **My Account**. You'll be prompted for it on first use."
        },
        "parley.defaultAgent": {
          "type": "string",
          "default": "bedrock/claude-sonnet-4-6",
          "markdownDescription": "Default model identifier (e.g. `bedrock/claude-sonnet-4-6`, `openai/gpt-5.1`, `google/gemini-3.1-pro`). Run `Parley: Set API Key` then pick from the model dropdown in the chat view."
        },
        "parley.stream": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Stream Parley replies token-by-token in the chat view."
        },
        "parley.thinking": {
          "type": "string",
          "default": "off",
          "enum": [
            "off",
            "adaptive",
            "low",
            "medium",
            "high"
          ],
          "enumDescriptions": [
            "No extended thinking (default)",
            "Adaptive — the model decides how much to reason (required for Claude Opus 4.7 on Bedrock)",
            "Enabled with a 4,096-token reasoning budget",
            "Enabled with an 8,192-token reasoning budget",
            "Enabled with a 16,000-token reasoning budget"
          ],
          "markdownDescription": "Extended thinking: makes the model reason step-by-step before answering, sent as the Parley `thinking` parameter. Supported on Claude, OpenAI reasoning models, and Gemini. Enabled levels also raise `max_tokens` to leave room for the answer. Thinking increases output-token usage (and cost). Also switchable from the **Mode** popover in the composer."
        },
        "parley.defaultMode": {
          "type": "string",
          "default": "chat",
          "enum": [
            "chat",
            "ask",
            "edit",
            "plan",
            "auto",
            "full"
          ],
          "enumDescriptions": [
            "Answer only — no agent, no file access",
            "Agent tools; approve each edit in a diff",
            "Agent tools; apply edits automatically (revertible)",
            "Agent reads the repo read-only and proposes a plan",
            "Agent decides and applies edits automatically",
            "CAUTION: auto-applies edits AND runs shell commands without asking"
          ],
          "markdownDescription": "Default chat **Mode**, also switchable from the Mode popover in the composer. Shell commands always require confirmation regardless of mode."
        },
        "parley.autoContinue": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "In agent modes, keep working automatically (up to a safety cap) until the task is complete, instead of stopping for you to say \"continue\". The agent signals completion with a `<DONE>` marker."
        },
        "parley.maxToolRounds": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "maximum": 200,
          "markdownDescription": "Maximum tool-call rounds the agent may take within a single turn."
        },
        "parley.maxAutoContinue": {
          "type": "number",
          "default": 25,
          "minimum": 0,
          "maximum": 200,
          "markdownDescription": "Maximum number of times Parley auto-continues a turn before pausing for you. `0` disables auto-continue."
        },
        "parley.tokenLimit": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Token budget per conversation. When the conversation's total token usage reaches this, Parley stops and asks you to raise the limit or start a new conversation. **`0` means unlimited (default).** Set it from `Parley: Set Token Limit`."
        },
        "parley.autoCompactTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "When the conversation's size exceeds this many tokens, automatically compact (summarize) it before the next turn to control cost and avoid context-window errors. The size is measured with Parley's `/v1/messages/count_tokens` endpoint when available, falling back to a character-based estimate. **`0` disables (default).** (Compaction is lossy — it replaces history with a summary.) The agent loop also trims stale tool outputs automatically regardless of this setting."
        },
        "parley.mcpServers": {
          "type": "object",
          "default": {},
          "markdownDescription": "Model Context Protocol (MCP) servers to launch and expose to the agent, keyed by name. Each value is `{ \"command\": string, \"args\"?: string[], \"env\"?: object }` (stdio transport). Their tools appear to the model as `mcp__<name>__<tool>`. Example: `{ \"filesystem\": { \"command\": \"npx\", \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \"${workspaceFolder}\"] } }`. Run **`Parley: Reconnect MCP Servers`** after editing.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "command": { "type": "string" },
              "args": { "type": "array", "items": { "type": "string" } },
              "env": { "type": "object", "additionalProperties": { "type": "string" } }
            },
            "required": ["command"]
          }
        },
        "parley.commandTimeoutSeconds": {
          "type": "number",
          "default": 300,
          "minimum": 5,
          "maximum": 3600,
          "markdownDescription": "How long a single agent shell command (`run_command`) may run before it's terminated. Default **300s** (5 min) so real `npm install` / build / test commands can finish. Raise it for slower builds."
        },
        "parley.autoSaveConversations": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically save each conversation as a Markdown file after every turn, so your history is preserved on disk. See `parley.conversationsDir` for the location, or run **`Parley: Open Conversations Folder`**."
        },
        "parley.conversationsDir": {
          "type": "string",
          "default": "",
          "markdownDescription": "Folder where conversations are auto-saved. Leave empty to use the extension's global storage folder (open it with **`Parley: Open Conversations Folder`**). Set an absolute path to keep transcripts somewhere you choose."
        },
        "parley.autoCompactPercent": {
          "type": "number",
          "default": 80,
          "minimum": 0,
          "maximum": 100,
          "markdownDescription": "Automatically compact the conversation when it reaches this percentage of the model's context window. **Defaults to `80`**, so long conversations are summarized before they overflow the context (the most recent messages are kept verbatim). Uses the model's known context size and exact token counting. Set to **`0` to disable**. Works alongside `parley.autoCompactTokens` — whichever triggers first."
        },
        "parley.inlineCompletion.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show Parley inline (ghost-text) code completions as you type. Toggle quickly with `Parley: Toggle Inline Completion`."
        },
        "parley.inlineCompletion.model": {
          "type": "string",
          "default": "openai/gpt-5-nano",
          "markdownDescription": "Model used for inline completions. Prefer a fast, inexpensive model."
        },
        "parley.inlineCompletion.debounceMs": {
          "type": "number",
          "default": 350,
          "minimum": 0,
          "markdownDescription": "Idle delay before requesting an inline completion, in milliseconds."
        },
        "parley.video.maxFrames": {
          "type": "number",
          "default": 12,
          "minimum": 1,
          "maximum": 60,
          "markdownDescription": "Maximum frames sampled from an attached video (evenly spread across its duration) and sent as images. Requires `ffmpeg` on PATH."
        },
        "parley.video.frameWidth": {
          "type": "number",
          "default": 768,
          "minimum": 128,
          "maximum": 2048,
          "markdownDescription": "Width (px) that sampled video frames are downscaled to before sending, to bound payload size. Aspect ratio is preserved."
        },
        "parley.video.maxAudioSeconds": {
          "type": "number",
          "default": 600,
          "minimum": 5,
          "maximum": 7200,
          "markdownDescription": "Maximum seconds of a video's audio track to extract when attaching audio, to bound payload size."
        },
        "parley.video.ffmpegPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the `ffmpeg` binary used for video frame/audio extraction. Leave empty to use `ffmpeg` from PATH. `ffprobe` is located beside it. Set this if PATH detection fails (common on Windows — point it at `ffmpeg.exe`)."
        },
        "parley.context.maxCharacters": {
          "type": "number",
          "default": 12000,
          "minimum": 1000,
          "markdownDescription": "Maximum characters of code/context to include in a request."
        },
        "parley.context.includeDiagnostics": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Include VS Code diagnostics when commands request problem context."
        },
        "parley.context.respectGitignore": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Respect workspace .gitignore patterns when collecting file context."
        },
        "parley.confirmBeforeSendingLargeContext": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show a context preview and confirmation before sending larger requests."
        },
        "parley.telemetry.enabled": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable extension telemetry. This extension currently sends no telemetry."
        },
        "parley.logLevel": {
          "type": "string",
          "default": "info",
          "enum": [
            "error",
            "warn",
            "info",
            "debug"
          ],
          "markdownDescription": "Controls extension output verbosity. Secrets and prompt contents are never logged."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\"",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\" \"media/*.{js,css}\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"test/**/*.ts\" \"media/*.{js,css}\"",
    "test": "npm run compile && node --test \"out/test/*.test.js\"",
    "test:integration": "npm run compile && node ./out/test/integration/runTest.js",
    "package": "vsce package --allow-missing-repository --no-rewrite-relative-links"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.14.12",
    "@types/vscode": "^1.92.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^2.32.0",
    "eslint": "^8.57.1",
    "glob": "^10.5.0",
    "mocha": "^10.8.2",
    "prettier": "^3.8.4",
    "typescript": "^5.5.4"
  }
}
